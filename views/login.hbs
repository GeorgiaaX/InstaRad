
{{!-- <!-- login Form -->
<div class="container login-container">
    <div class="row justify-content-center align-items-center">
        <!-- Hide the image on smaller screens -->
        {{!-- <div class="col-md-7 d-sm-none d-md-block">
            <img src="/images/phoneImg.jpg" alt="Phone Image" class="img-fluid">
        </div> --}}
        <div class="col-12 col-md-5 mb-4">
            <div class="container text-center login-form-container">
                <div class="card">
                    <div class="card-body">
                        <h1 class="card-title brand-font">InstaRad</h1>
                        {{#if validationErrors}}
                        <!-- Display validation errors -->
                        <div class="alert alert-danger">
                            <ul>
                                {{#each validationErrors}}
                                <li class="list-unstyled">{{this.msg}}</li>
                                {{/each}}
                            </ul>
                        </div>
                        {{/if}}
                        <div class="card-body">
                            <form action="/login" method="POST">
                                <div class="form-floating mb-3">
                                    <input class="form-control" type="email" id="email" name="email" placeholder="Email" required>
                                    <label for="email">Email</label>
                                </div>
                                <div class="form-floating mb-3">
                                    <input class="form-control" type="password" id="password" name="password" placeholder="Password" required>
                                    <label for="password">Password</label>
                                </div>
                                <button type="submit" class="btn btn-primary w-100">Login</button>
                            </form>
                        </div>
                        <span>Don't have an account? <a href="/signUp" class="text-decoration-none">Sign Up</a></span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>  --}}


   <div class="container">
        <div class="row">
            <div class="col-12">
                <h1 class="text-center">Login</h1>
                <div class = "card">
                    <div class="card-body">
                        <form action="/login" method="POST">
                                <div class="form-floating mb-3">
                                    <input class="form-control" type="email" id="email" name="email" placeholder="Email" required>
                                    <label for="email">Email</label>
                                </div>
                                <div class="form-floating mb-3">
                                    <input class="form-control" type="password" id="password" name="password" placeholder="Password" required>
                                    <label for="password">Password</label>
                                </div>
                                <button type="submit" class="btn btn-primary w-100">Login</button>
                            </form>
                        </div>
                     </div>
                    <span>Don't have an account? <a href="/signUp" class="text-decoration-none">Sign Up</a></span>
                </div>
            </div>
        </div>
    </div>